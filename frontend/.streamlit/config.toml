[server]
# Max upload size per file in MB (default is 200)
maxUploadSize = 3048

# Max total message size in MB (for multiple files at once)
maxMessageSize = 3048

# Enable XSRF protection
enableXsrfProtection = true

[browser]
# Gather usage stats
gatherUsageStats = false
